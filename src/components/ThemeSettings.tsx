
import React from 'react';

interface ThemeSettingsProps {
  isVisible: boolean;
  onClose: () => void;
}

const ThemeSettings: React.FC<ThemeSettingsProps> = ({ isVisible, onClose }) => {
  // Component removed - theme settings no longer needed
  return null;
};

export default ThemeSettings;
